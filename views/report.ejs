<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Attendance Report</title>
</head>
<body>
    <h1>Attendance Report</h1>
    <table class="report-table">
        <thead>
            <tr>
                <th>Student Name</th>
                <th>Email Address</th>
                <th>Course Enrolled</th>
                <th>Lecture Join Time</th>
                <th>Lecture Leave Time</th>
                <th>Attendance Status</th>
            </tr>
        </thead>
        <tbody>
            <% reportData.forEach(record => { %>
                <tr>
                    <td><%= record.studentName %></td>
                    <td><%= record.emailAddress %></td>
                    <td><%= record.courseEnrolled %></td>
                    <td><%= record.lectureJoinTime ? record.lectureJoinTime.toLocaleString() : 'N/A' %></td>
                    <td><%= record.lectureLeaveTime ? record.lectureLeaveTime.toLocaleString() : 'N/A' %></td>
                    <td><%= record.attendanceStatus %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>